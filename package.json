{
  "name": "carousel-post-builder",
  "version": "1.0.0",
  "private": true,
  "description": "図解ビルダーPRO MAX — LIFFで認証する静的サイト + Vercel上の中央認証API (/api/me)",
  "engines": {
    "node": ">=20.x"
  },
  "scripts": {
    "build:wp": "node build-wp.js",
    "dev": "vercel dev",
    "deploy": "vercel deploy"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0"
  }
}
